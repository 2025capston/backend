package com.capston.matching_app.repository;

import com.capston.matching_app.entity.MatchTimeOption;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MatchTimeOptionRepository extends JpaRepository<MatchTimeOption, Long> {
}
