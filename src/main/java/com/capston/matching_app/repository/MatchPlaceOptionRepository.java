package com.capston.matching_app.repository;

import com.capston.matching_app.entity.MatchPlaceOption;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MatchPlaceOptionRepository extends JpaRepository<MatchPlaceOption, Long> {
}
